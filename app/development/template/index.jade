doctype html
html(lang="ja")
    head
        meta(charset="utf-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        title
        meta(name="description", content="")
        meta(name="keywords", content="")
        meta(name="author", content="")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="icon", href="{{ static_url('img/favicon.ico') }}")
        link(href="{{ static_url('css/materialize.min.css') }}", rel="stylesheet" media="screen,projection")
        link(href="{{ static_url('css/app.css') }}", rel="stylesheet")
    body
        script(src="{{ static_url('js/jquery.min.js') }}")
        script(src="{{ static_url('js/materialize.min.js') }}")
        script(src="{{ static_url('js/knockout.js') }}")
        script(src="{{ static_url('js/app.js') }}")
